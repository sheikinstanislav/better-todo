import { FC } from 'react';
import './header.scss';

export const Header: FC = () => {
  return (
    <header className="header bg-slate-700 text-yellow-50">
      <div className="container">header</div>
    </header>
  );
};
